<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CompressStud" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_700</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="buttonStyle">@drawable/button</item>
        <item name="android:textColor">@color/black</item>
        <!-- Custom elements colors -->
        <item name="whiteButtonBackground">@color/white</item>
        <item name="modalBackground">@color/white</item>
        <item name="sectionBackground">@color/gray</item>
        <item name="spinnerBackground">@color/white</item>
        <item name="inputBackground">@color/gray</item>
        <item name="settingsOption">@color/white</item>
    </style>

    <style name="Theme.CompressStud.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.CompressStud.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.CompressStud.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>